const express=require('express');const app=express();app.use(express.json());let products=[{id:1,name:'Keyboard',price:1200}];app.get('/products',(req,res)=>res.json(products));app.get('/products/:id',(req,res)=>res.json(products.find(p=>p.id==req.params.id)));app.post('/products',(req,res)=>{products.push(req.body);res.status(201).json(req.body);});app.put('/products/:id',(req,res)=>{products=products.map(p=>p.id==req.params.id?req.body:p);res.json({message:'Updated'});});app.delete('/products/:id',(req,res)=>{products=products.filter(p=>p.id!=req.params.id);res.json({message:'Deleted'});});app.listen(3000);