const express=require('express');const app=express();app.use(express.json());let students=[{id:1,name:'Asha',dept:'CSE'}];app.get('/students',(req,res)=>res.json(students));app.post('/students',(req,res)=>{students.push(req.body);res.json({message:'Created'});});app.put('/students/:id',(req,res)=>{students=students.map(s=>s.id==req.params.id?req.body:s);res.json({message:'Updated'});});app.delete('/students/:id',(req,res)=>{students=students.filter(s=>s.id!=req.params.id);res.json({message:'Deleted'});});app.listen(3000);